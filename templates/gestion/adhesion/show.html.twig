{% extends 'admin.html.twig' %}

{% block title %}Adhesion{% endblock %}

{% block breadcrumb_content %}
    {{ include('admin/include/_breadcrumb.html.twig', {
        firstLabel : 'Adhesion',
        firstLabel_route: 'mac_gestion_adhesion_index',
        secondLabel : 'Index',
        secondLabel_route: null,
        thirdLabel : null,
        thirdLabel_route: null,
    }) }}
{% endblock %}

{% block header_content %}
    <div class="row">
        <div class="col-12">
            <h2>Voir l'adhésion</h2>
        </div>
    </div>
{% endblock %}

{% block main_content %}
    <h1>Adhesion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adhesion.id }}</td>
            </tr>
            <tr>
                <th>Cotisation</th>
                <td>{{ adhesion.cotisation }}</td>
            </tr>
            <tr>
                <th>IsPaid</th>
                <td>{{ adhesion.isPaid ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsFree</th>
                <td>{{ adhesion.isFree ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>PaidAt</th>
                <td>{{ adhesion.PaidAt ? adhesion.PaidAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PaidBy</th>
                <td>{{ adhesion.paidBy }}</td>
            </tr>
            <tr>
                <th>RefPaid</th>
                <td>{{ adhesion.refPaid }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ adhesion.createAt ? adhesion.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ adhesion.updateAt ? adhesion.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mac_gestion_adhesion_index') }}">back to list</a>

    <a href="{{ path('mac_gestion_adhesion_edit', {'id': adhesion.id}) }}">edit</a>

    {{ include('gestion/adhesion/_delete_form.html.twig') }}
{% endblock %}
