<form method="post" action="{{ path('app_gestion_adhesions_campaign_adhesion_delete', {'id': campaign_adhesion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ campaign_adhesion.id) }}">
    <button class="btn">Delete</button>
</form>
