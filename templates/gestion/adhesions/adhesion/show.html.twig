    <h1>Adhesion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adhesion.id }}</td>
            </tr>
            <tr>
                <th>PriceCotisation</th>
                <td>{{ adhesion.priceCotisation }}</td>
            </tr>
            <tr>
                <th>PayBy</th>
                <td>{{ adhesion.payBy }}</td>
            </tr>
            <tr>
                <th>PayAt</th>
                <td>{{ adhesion.payAt ? adhesion.payAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>StartAt</th>
                <td>{{ adhesion.startAt ? adhesion.startAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>FinishAt</th>
                <td>{{ adhesion.finishAt ? adhesion.finishAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ adhesion.createdAt ? adhesion.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ adhesion.updatedAt ? adhesion.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_adhesions_adhesion_index') }}">back to list</a>

    <a href="{{ path('app_gestion_adhesions_adhesion_edit', {'id': adhesion.id}) }}">edit</a>

    {% if is_granted('ROLE_SUPER_ADMIN') %}
        {{ include('gestion/adhesions/adhesion/_delete_form.html.twig') }}
    {% endif %}