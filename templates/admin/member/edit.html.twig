{% extends 'admin.html.twig' %}

{% block title %}Edit Member{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="module" src="{{ asset('js/admin/member/new_edit.js') }}"></script>
{% endblock %}

{% block breadcrumb_content %}
    {{ include('admin/include/_breadcrumb.html.twig', {
        firstLabel : 'Membre',
        firstLabel_route: 'mac_admin_member_index',
        secondLabel : 'Editer une fiche',
        secondLabel_route: null,
        thirdLabel : null,
        thirdLabel_route: null,
    }) }}
{% endblock %}

{% block header_content %}
    <div class="row">
        <div class="col-12">
            <h2>Modifier la fiche de : <span class="text-muted">{{ member.firstName }} {{ member.lastName }}</span></h2>
        </div>
    </div>
{% endblock %}

{% block main_content %}

    <div class="row">
        <div class="col">
            <div class="float-start">
                <a class="btn btn-sm btn-outline-dark" href="{{ path('mac_admin_member_index') }}"><i class="fa-regular fa-chevrons-left"></i> Retour</a>
                <a class="btn btn-sm btn-outline-dark submitForm" href="{{ path('mac_admin_member_edit', {'id': member.id}) }}" data-target="formMember"><i class="fa-duotone fa-solid fa-pen-circle"></i> Modifier</a>
            </div>
            <div class="float-end">
                {{ include('admin/member/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

    {{ include('admin/member/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

{% endblock %}
