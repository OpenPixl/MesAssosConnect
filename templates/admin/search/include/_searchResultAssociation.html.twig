{% for member in members %}
    <a href="{{ path('mac_admin_adherent_add', {'idMember': member.id, 'idAsso': idAsso}) }}" class="block px-2 py-1 text-right text-slate-900 hover:text-sky-700 hover:bg-gray-200 focus:bg-gray-200 result" role="menuitem">
        <div class="flex items-center justify-between">
            <i class="fa-light fa-user-plus"></i>
            <div>
                <span class="text-sm">{{ member.firstName }} {{ member.lastName }}</span><br><span class="text-xs text-slate-500">{{ member.zipcode }} {{ member.city }}</span>
            </div>
        </div>
    </a>
{% else %}
    <div class="grid grid-cols-1 px-2 p-1">
        <p>{{ members|length }} utilisateur trouvé.</p>
    </div>
{% endfor %}